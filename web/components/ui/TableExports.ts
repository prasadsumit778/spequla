/** Re-export of the table primitives under the names screens use, so a
 *  screen imports one module rather than six. */
export { Table, TBody, TD, TH, THead, TR, TableFrame as TFrame } from "./Table";
