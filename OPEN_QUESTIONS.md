# Open Questions

Escalations land here, in the format specified in CLAUDE.md section 4. A
resolved item keeps its original escalation text (nothing is ever deleted,
per CLAUDE.md invariant 4) with a **Resolved** block appended stating the
decision, who made it, and what changed. Resolved below: OQ-001, OQ-002,
OQ-003, OQ-005, OQ-006, OQ-007, OQ-009, and OQ-010 through OQ-018.
OQ-004 is partially resolved. Still open: OQ-008 and OQ-019.
The nine resolutions dated 2026-08-31 (OQ-010 to OQ-018) are decisions on
record whose code changes have NOT been applied -- each carries a "Not yet
applied" line naming what it changes, and none of it is written yet.

## OQ-001  Auto-accept rupee ceiling undeclared
Raised: 2026-08-20   Task: Sprint 2, mapping engine auto-accept gate (corpus/06 section 4.2)
Blocked by: corpus gap
What I need: corpus/06 section 4.2 requires auto-accept to check "the account's period value below a declared rupee ceiling," but names no value and no decision id governing it -- it isn't one of the 12 open DECISION-REQUIRED items in corpus/00 either, it's simply undeclared.
Options I can see: (a) treat it as a per-company setting, declared during onboarding, same shape as D-052/D-053; (b) it was meant to be assigned a decision id (e.g. a D-06x) and that id is missing from corpus/00; (c) some other resolution I'm not positioned to guess.
What I will NOT do: pick a number and proceed.
Downstream blocked: `src/mapping/engine.py`'s `evaluate_auto_accept()` currently runs with this condition skipped (`rupee_ceiling=None`) rather than defaulted -- every other auto-accept condition (exact rule match, not a judgement class, no conflicting prior mapping) still applies in full, so this does not weaken the judgement-class exclusion, but it does mean a very large exact-rule-matched account could auto-accept today where a real declared ceiling might route it to manual review instead.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** D-065 (corpus/00): flat ₹1,00,000 per period, not a percent of revenue -- revenue is a derived metric that needs a frozen mapping version to compile, which doesn't exist yet at auto-accept time, so a revenue-relative ceiling would be circular. `AUTO_ACCEPT_RUPEE_CEILING_INR` in `src/mapping/engine.py` is now the default for `evaluate_auto_accept()` and `run_mapping_pass()`. corpus/06 section 4.2 updated.

## OQ-002  debt metric formula does not match its own governing decision
Raised: 2026-08-21   Task: Sprint 3, deterministic compiler (corpus/05 metric registry, `debt`)
Blocked by: conflict within the corpus (corpus/05 vs corpus/00 D-035, same metric)
What I need: corpus/05's `debt` row gives the formula as `gl_class(liability.debt_*)`, a prefix match. That pattern matches `liability.debt_term`, `debt_working_capital`, `debt_current_maturity` and `debt_related_party`, but NOT `liability.bill_discounting` (different prefix). D-035's own resolution text (corpus/00 section 2b) says debt "includes term loans, cash credit and overdraft, current maturities, bill discounting (per D-036) and unsecured related-party loans" -- explicitly including bill discounting. The formula and the decision it is governed by disagree on the same metric.
Options I can see: (a) the formula should be `gl_class(liability.debt_*|liability.bill_discounting)` and the corpus's formula string is the stale artifact; (b) D-035's resolution prose is the aspirational/prose description and the formula's narrower pattern is the deliberately-scoped literal definition (e.g. bill discounting is meant to surface only via the separate `liability.bill_discounting` judgement class review, never folded into the automated `debt` roll-up); (c) some other resolution.
What I will NOT do: silently widen or narrow the formula pattern to match what I think D-035 "meant."
Downstream blocked: `src/semantic/compiler.py`'s leaf resolver for the `debt` metric implements the formula exactly as written in corpus/05 (`liability.debt_*`, excluding bill discounting) -- so `debt` and everything derived from it (`net_debt`) will run 2-5 percentage points light on any company that carries bill-discounted receivables, until this is resolved.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** D-035's resolved text is authoritative; the formula was the stale artifact. corpus/05's `debt` row updated to `gl_class(liability.debt_*|liability.bill_discounting)`, `config/metrics/debt.yml` regenerated via `scripts/gen_metric_contracts.py`. `match_gl_classes` (`src/semantic/formula.py`) already supports `|`-separated exact classes mixed with a wildcard within one `gl_class(...)` argument, so no compiler code changed.

## OQ-003  Admission control cost cap and row cap undeclared
Raised: 2026-08-21   Task: Sprint 4, admission control gates (corpus/07 section 7)
Blocked by: corpus gap
What I need: corpus/07 section 7 gate 6 is "Cost estimate. Under the configured cap" and gate 7 is "Row cap. Applied" -- neither names a number, and neither is one of the 12 open DECISION-REQUIRED items in corpus/00. Same shape as OQ-001: a real threshold the corpus clearly intends to exist but never states.
Options I can see: (a) a per-tenant or global config value, set during onboarding, same shape as D-052/D-053; (b) a decision id was meant to cover this and is missing from corpus/00; (c) some other resolution.
What I will NOT do: invent a cost or row number and present it as policy.
Downstream blocked: `src/semantic/admission.py`'s `gate_6_cost_estimate` never rejects on cost (cap=None is a structural no-op, not a defaulted pass). `gate_7_row_cap` applies a LIMIT since corpus/07 says the row cap must be "applied," not merely checked -- unlike gate 6, silently returning an unbounded result set to a browser is its own operational risk. `DEFAULT_ROW_CAP_PENDING_DECLARATION = 10,000` is labelled in the code as existing only to prevent that, explicitly not a business rule, and should be replaced the moment a real cap is declared.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** D-066: gate 6's cap is ₹5 per query of estimated AI model spend (not a row count -- that was gate 7's job, conflated before this resolution). D-067: gate 7's row cap confirmed at 10,000 (renamed `ROW_CAP`, no longer "pending declaration"). `src/semantic/admission.py` updated: `gate_6_cost_estimate` now takes `estimated_cost_inr: Decimal` against `COST_CAP_INR_PER_QUERY`. The cap is real now, but `src/semantic/ask.py` still passes no cost estimate into `run_admission_gates` -- that's a separate, still-open gap: `AnthropicModelClient` (`src/semantic/model_client.py`) remains an explicitly unconfigured connection point with no token usage to compute a cost from, unrelated to this OQ.

## OQ-004  Eight cash flow leaf metrics have no formula or canonical class anywhere in the corpus
Raised: 2026-08-21   Task: Sprint 5, indirect cash flow statement (corpus/08 section 6, corpus/03 section 4)
Blocked by: corpus gap
What I need: corpus/03 section 4 gives exact formulas for `operating_cash_flow`, `investing_cash_flow` and `financing_cash_flow` in terms of named components -- but eight of those components (`taxes_paid`, `asset_disposals`, `investment_movements`, `debt_drawn`, `debt_repaid`, `equity_raised`, `dividends`, `interest_paid`) have no metric row in corpus/05, no gl_class formula, and no canonical class in corpus/06's taxonomy anywhere. This is not the same shape as OQ-001/OQ-003 (an acknowledged-but-unquantified threshold) -- these are formula inputs with no definition at all, not even a placeholder. By contrast `capex`, `pbt`, `da`, `interest_expense`, `other_income` and `working_capital_change` (the other components of the same three formulas) are all fully specified with literal `gl_class(...)` or `delta gl_class(...)` expressions, which is why I'm confident the other eight are a real gap and not something I'm missing.
A secondary, smaller inconsistency in the same area: corpus/03 section 4 cites "D-037. Interest paid versus interest expense differ where interest is accrued but unpaid" as the decision governing that distinction, but D-037's actual resolved text (corpus/00, section E) is "Capex measured as additions to gross block plus capital WIP movement, not cash paid" -- a different subject entirely. Either the citation is stale or a decision id is missing.
Options I can see: (a) these eight are meant to be computed from balance sheet deltas the same way `capex` is (e.g. `debt_drawn`/`debt_repaid` as split signs of the net delta across the debt classes in D-035's list, `investment_movements` as delta of `asset.investment`, `equity_raised` as delta of `equity.share_capital`) -- plausible but nowhere stated, and gross drawn/repaid specifically cannot be recovered from a net period-end balance if both a draw and a repayment happened in the same month; (b) they require a transactional feed this system does not ingest (bank statement doesn't tag purpose, no loan register, no dividend/tax-payment source) and are genuinely out of P0 despite `p0_statement: yes` on `investing_cash_flow`/`financing_cash_flow` in corpus/05; (c) some other resolution.
What I will NOT do: derive `debt_drawn`/`debt_repaid`/`equity_raised`/`dividends`/`interest_paid`/`asset_disposals`/`investment_movements`/`taxes_paid` from balance sheet deltas on my own judgement and present the result as the defined metric.
Downstream blocked: `src/reports/cashflow.py` computes `working_capital_change` in full (fully specified, itemised by receivables/inventory/payables/other operating per corpus/08 section 6) and the four fully-specified components of `operating_cash_flow` (`pbt`, `da`, `interest_expense`, `other_income`), but reports `operating_cash_flow` itself, `investing_cash_flow`, `financing_cash_flow`, `net_cash_movement` and `closing_cash` as not configured. Per corpus/08 section 5's own hard gate ("closing_cash must equal balance sheet cash exactly, or neither displays"), the cash flow statement therefore does not display in the pack or the statements screen until this resolves -- it is reported as a known gap in every pack's section 9, not hidden.

**Partially resolved 2026-08-24 by Bhavya Singhal, founder.** Decision: derive what's derivable from balance-sheet deltas, using classes the taxonomy already has, and explicitly leave the rest undefined rather than guess. Two of the eight terms now have a formula: `investment_movements` (delta of `asset.investment`) and `equity_raised` (delta of `equity.share_capital`), both implemented in `src/reports/cashflow.py` and documented in corpus/03 section 4. The remaining six (`taxes_paid`, `asset_disposals`, `debt_drawn`, `debt_repaid`, `dividends`, `interest_paid`) stay undefined by explicit decision: `debt_drawn`/`debt_repaid` can't be split from `debt`'s single net period-end delta (a draw and a repayment in the same month collapse to one number, same structural problem as `asset_disposals` against `fixed_asset_gross`'s net-only movement), and `taxes_paid`/`dividends`/`interest_paid` have no payable-side canonical class (no `liability.tax_payable`, `liability.dividend_payable` or `liability.interest_payable` in the taxonomy) to derive an accrual adjustment from. `operating_cash_flow`, `investing_cash_flow`, `financing_cash_flow` and `closing_cash` therefore still do not reconcile and the cash flow statement still does not display -- `reconciles=False` is unchanged, only the disclosed detail improved. Still open: whether to add the missing payable-side canonical classes (a taxonomy change, not a corpus-reading exercise) or declare the remaining six formally out of P0 scope.

## OQ-005  Tenant data retention period undeclared
Raised: 2026-08-21   Task: Sprint 7, tenant deletion path (corpus/12 sprint 7: "Retention and deletion paths")
Blocked by: corpus gap
What I need: the backlog names "retention and deletion paths" as sprint 7 database work, and corpus/02 section 7 says employee access is "time-bound," but no corpus file states how long a tenant's data is kept after a pilot ends, or what triggers deletion (an explicit offboarding request is the only trigger I can be confident about) -- there is no D-number for this at all, unlike D-052/D-053's acknowledged-but-unset thresholds.
Options I can see: (a) deletion is request-triggered only, no automatic time-based purge -- what I built; (b) a fixed retention window after pilot end (e.g. 90 days) with automatic purge after, which needs a number nobody has stated; (c) some other resolution.
What I will NOT do: invent a retention period and build a scheduled job to enforce it.
Downstream blocked: `src/admin/tenant_lifecycle.delete_tenant()` is request-triggered only (named requester, named reason, both required) -- there is no background job anywhere that deletes a tenant automatically after any elapsed time. If a time-based policy is later declared, that is new scheduled-job code, not a change to this function.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** D-068: confirmed as the deliberate policy, no code change (this was already the built behaviour). `delete_tenant()`'s docstring updated to cite D-068 rather than describe an open gap.

## OQ-006  No canonical GL class exists for marketplace revenue earned
Raised: 2026-08-22   Task: eval suite, consumer statements tie-out (corpus/08 section 5, corpus/06 section 3)
Blocked by: corpus gap
What I need: corpus/06's canonical class taxonomy has exactly one marketplace-commission class, `contra_revenue.commission_marketplace`, and it is the commission a seller *bears* under the buyout model (D-004: "commission borne is a cost above gross profit"). There is no class anywhere in corpus/06 for the commission, advertising and platform fee a marketplace operator *earns* -- even though D-004's own second sentence ("commission earned under the marketplace model is revenue") and D-061 ("Marketplace: revenue is commission plus advertising earnings plus platform fee") both say that income is revenue. corpus/04 section 3 carries `commission_earned`, `advertising_earned` and `platform_fee_earned` as columns on `fact_channel_order_line`, and corpus/05a's `gmv` contract computes marketplace revenue from those three columns -- so the *metric* path is fully specified. The *GL* path is not: a marketplace operator's books still contain an income ledger for that same money, and no canonical class accepts it.
Options I can see: (a) corpus/06's taxonomy is missing a class (e.g. `revenue.commission_marketplace` / `revenue.platform_fee`) and should gain one; (b) such a ledger is meant to map to an existing revenue class -- `revenue.service_income` and `revenue.other_operating` are both defensible and I am not positioned to choose between them; (c) a marketplace operator's commission income is deliberately never taken from the GL, only from the order file, in which case the mapping spec needs to say what the GL ledger maps to anyway, because the trial balance still contains it; (d) some other resolution.
What I will NOT do: pick a revenue class for marketplace commission income on my own judgement. Which line a company's revenue lands on is exactly the kind of classification CLAUDE.md section 3.1 forbids me from inventing.
Downstream blocked: the synthetic consumer company posts real GL credits to a "Marketplace Revenue Earned" ledger (`synthetic/consumer/engine.py`). With no matching canonical class the mapping engine correctly defers it to the review queue, where it stays `suspense.unmapped`. Mapping coverage is 99.96% by value, which clears the freeze gate, but the consumer balance sheet is then out by exactly that unmapped amount and is correctly NOT displayed per CLAUDE.md invariant 9. `tests/eval/test_statements_tie_consumer.py` therefore fails on its balance assertion -- the system is behaving correctly and the reference dataset cannot tie until this class is declared.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** New canonical class `revenue.commission_marketplace` added to corpus/06a's Canonical Classes tab (statement_section P&L, statement_line gross_revenue, profile Consumer, decisions D-004|D-061), distinct from the existing `contra_revenue.commission_marketplace` (commission a seller BEARS under buyout, D-004) -- this new class is commission/advertising/platform-fee income a marketplace operator EARNS. `config/taxonomy.yml` regenerated (83 classes, up from 82). `src/mapping/rules.py` gained an exact rule routing "Marketplace Revenue Earned" to the new class. Deliberately NOT wired into `src/reports/statement_lines.py`'s `CONSUMER_GROSS_REVENUE_CLASSES` -- that list also feeds the CM ladder's `gmv` line (`gmv = gross_revenue + gst`), and folding commission income into GMV would misstate GMV as merchandise value, a modelling question corpus/08 section 4.1 doesn't resolve and D-061 doesn't answer at the GL-presentation level (only at the metric-contract level, via `fact_channel_order_line`). The ledger still ties out on the balance sheet regardless (see OQ-007's resolution, below) since any mapped P&L-side class flows into retained earnings automatically. Whether/how to also present it on the consumer P&L ladder is a new, narrower follow-up, not decided here.

## OQ-007  A balance sheet cannot balance while any account sits in suspense, but D-053 says small unmapped value is fine
Raised: 2026-08-23   Task: eval suite, statements tie-out (corpus/08 section 5, corpus/06 section 3.6, D-053)
Blocked by: conflict within the corpus (D-053 / corpus/06 section 3.6 vs CLAUDE.md invariant 9 and corpus/08 section 5)
What I need: the trial balance sums to exactly zero across every class (D-051, zero tolerance). So for any period, sum(balance sheet classes) + sum(P&L classes) + sum(suspense.unmapped) = 0. `src/reports/balance_sheet.compute_balance_sheet` derives retained earnings as the negative of the cumulative P&L total, which is the correct closing entry, and excludes `suspense.unmapped` from both sides because corpus/06's taxonomy gives that class `statement_section: memo`, not `bs`. The arithmetic consequence is exact and unavoidable: assets minus liabilities-and-equity equals the signed suspense balance. **Any company with a single unmapped account carrying a non-zero balance can therefore never display a balance sheet**, because CLAUDE.md invariant 9 and corpus/08 section 5 forbid displaying one that does not balance.
That collides with D-053, which is resolved and says unmapped value is "silent below 0.5%" of period value and only blocks above 2%. D-053 clearly contemplates a company operating normally with a small unmapped tail; corpus/06 section 3.6 describes exactly that ("the long tail sits in a suspense class, visible and quantified in rupees, until someone cares"). Both statements cannot hold at once while suspense is a memo class: below 0.5% the corpus says carry on, and invariant 9 says the balance sheet is withheld.
Options I can see: (a) `suspense.unmapped` should appear as its own line on the balance sheet rather than as a memo, so the sheet balances with the unclassified amount visible on its face -- this is the only option that satisfies D-053, corpus/06 section 3.6 and invariant 9 simultaneously, but it contradicts the `statement_section: memo` that corpus/06's own taxonomy assigns it; (b) the balance sheet is genuinely meant to be withheld until the mapping is 100% complete by value, and D-053's silent/badge bands govern only metric serving and pack generation, never the balance sheet -- in which case corpus/02 section 8's "98 percent of period value mapped before statements unlock" is the statement that needs rewording, because 98% is not enough to display a balance sheet and never can be; (c) retained earnings should be derived from the trial balance including suspense rather than from mapped P&L classes only, which would balance the sheet by treating unclassified value as a P&L residual -- an accounting treatment no corpus file states; (d) some other resolution.
What I will NOT do: move `suspense.unmapped` onto the balance sheet, or fold it into retained earnings, on my own judgement. Where an unclassified balance is presented is a financial-statement classification decision, which CLAUDE.md section 3.1 forbids me from inventing, and either choice changes a published balance sheet.
Downstream blocked: both synthetic reference companies carry a deliberately unmappable long tail (corpus/11 section 2.2 seeds it as defect 10), so neither balance sheet balances and neither displays -- correct behaviour today, and the system reports the gap rather than hiding it. `tests/eval/test_statements_tie_manufacturer.py` and `tests/eval/test_statements_tie_consumer.py` now assert the provable property instead: that the imbalance equals the signed suspense balance exactly, so the gap is fully explained and nothing is unaccounted for. They should be changed back to asserting `bs.balances` the moment this is resolved. The consumer company is additionally blocked by OQ-006.

**Resolved 2026-08-24 by Bhavya Singhal, founder.** Option (a): `suspense.unmapped` gets its own balance sheet line rather than being excluded from both sides. corpus/06a's Canonical Classes tab updated: `statement_section` for `suspense.unmapped` changed MEMO -> BS (statement_line: unclassified); `config/taxonomy.yml` regenerated. `src/reports/statement_lines.py`'s `BALANCE_SHEET_LINES` gained `"suspense.unmapped": ("unclassified", "Unclassified (suspense)", REVENUE_SIGN)` -- the sign is the one that makes `total_assets == total_liabilities_and_equity` hold by construction (derived from the trial balance's own zero-tolerance identity, D-051), not a copy of the liability presentation convention. `src/reports/balance_sheet.py`'s `compute_balance_sheet` no longer `continue`s past `suspense.unmapped` before adding it to a group; `total_liabilities_and_equity` now includes the new `unclassified` group total. `unmapped_value_inr` (D-053's badge/block input) is still tracked independently, unaffected. Net effect: the balance sheet now balances whenever the trial balance does (always, D-051), regardless of how much sits in suspense, with the unmapped amount visible in rupees on the face of the sheet rather than causing it to be withheld. Both eval tests updated accordingly: `test_statements_tie_consumer.py` now asserts `bs.balances is True` outright (OQ-006 leaves nothing in that company's suspense at all, and it has no genuine trial-balance-breaking defect). `test_statements_tie_manufacturer.py` still asserts `bs.balances is False`, but for a narrower reason than before -- defect #4 (corpus/11 section 2.2) deliberately posts one genuinely unbalanced voucher, independent of suspense, so that company's trial balance itself is nonzero regardless of this fix; its identity assertion is updated to drop the now-cancelled `- suspense_signed` term. Neither test change has been run against a live Postgres in this session -- verify with `pytest tests/eval -k statements_tie` before relying on it.

## OQ-008  D-065's auto-accept ceiling is declared "per period" but is compared against all-history cumulative movement
Raised: 2026-08-24   Task: repairing the four test failures caused by the 2026-08-24 decision resolutions (corpus/00 section 2b, corpus/06 section 4.2)
Blocked by: DECISION-REQUIRED[D-065] | conflict between D-065's stated basis and `src/mapping/engine.py`'s implementation
What I need: is D-065's ₹1,00,000 ceiling measured against one month's movement, twelve months' movement, or all movement ingested to date?
The number is declared and unambiguous; the basis it is measured on is not, and the three candidate bases differ by more than an order of magnitude on the reference company. `corpus/06` section 4.2 states the condition as "the account's period value below a declared rupee ceiling (D-065: flat ₹1,00,000 per period)". `src/mapping/engine.py`'s `evaluate_auto_accept` compares against `AccountToMap.period_value_inr`, which `extract_coa`'s own docstring defines as "the sum of absolute `amount_base` movement across every ingested fact for that account", noting that this sprint ingests full history in one pass so corpus/06 section 4.1's "twelve months of movement" "collapses to 'all movement seen so far'". For the synthetic manufacturer that is 36 months, so the gate is currently roughly three times stricter than a twelve-month reading and thirty-six times stricter than a monthly one — and the divergence grows with every month ingested, which means the same ledger auto-accepts today and stops auto-accepting later without anything about it changing.
Measured on the reference company (₹100 Cr revenue in year one, 36 months ingested): 77 accounts match an exact rule; 24 fall below the ceiling and auto-accept, 53 go to human review. The median rule-matched ledger carries ₹1.34 Cr of cumulative movement, 134x the ceiling; "Sales - Direct (North)" carries ₹98.4 Cr, 9,838x. Even read as a single month, the median ledger is roughly ₹37 lakh, still 37x the ceiling. So on any of the three bases a flat ₹1,00,000 auto-accepts only the near-dormant tail of a mid-market company's ledger, and every material account is a human's confirmation. That may well be the intent — it is the conservative reading, and it is consistent with this system's purpose — but it is a large change in what the auto-accept loop is for, it was not stated as the intent in D-065, and it should be confirmed rather than assumed from a number that was chosen to answer a different question.
Options I can see: (a) per calendar month, the strictest and most stable reading, and the only one where a ledger's auto-accept eligibility does not drift as history accumulates; (b) twelve months of movement, matching what corpus/06 sections 4.1 and 5 already say the proposer and the review queue are shown, at the cost of the ceiling meaning something different in month three than in month thirteen; (c) all movement ingested to date, the current behaviour, which makes the gate strictly tighten over a tenant's life; (d) the ceiling is confirmed as intentionally admitting only immaterial ledgers on the current basis, in which case corpus/06 section 4.2's "per period" wording should be corrected to say so.
What I will NOT do: pick a basis and normalise `period_value_inr` to it. Which window a materiality ceiling is measured over is a threshold decision, and CLAUDE.md section 3.2 forbids inventing one; dividing by 36 to "make it per period" would be exactly that.
Downstream blocked: nothing is blocked from running. `tests/integration/test_mapping_auto_accept_judgement_class.py::test_non_judgement_exact_matches_do_auto_accept` was rewritten to assert the acceptance *rule* against whatever basis is in force rather than a fixed count, so it passes under any resolution and will not need changing again unless the rule itself changes. What is affected is the review queue's size on a real onboarding: under the current basis a first mapping run hands a human roughly two thirds of the material ledger, which is a workload question worth knowing before a pilot rather than during one.

## OQ-009  A load touching an already-validated period has no stated transition
Raised: 2026-08-30   Task: Sprint 3, wiring OPEN -> VALIDATED into `load_gl_file` (corpus/09 section 5)
Blocked by: corpus gap
What I need: what should a GL load do when it touches a period that already carries a period_lock row -- usually VALIDATED, sometimes MAPPED?
`src/quality/period_state.validate_period` refuses it today, and refuses correctly on the corpus as written: corpus/09 section 5 draws five arrows and no self-arrow on any state, so VALIDATED -> VALIDATED is not a transition the diagram admits, and `_PREDECESSOR` implements that diagram literally. But a second GL file for the same month is ordinary usage, not an error -- a corrected export, a month re-sent after the client posts late vouchers, or the trailing ninety-day re-pull that corpus/09 section 5 itself calls "the normal case in Indian accounting, not an exception." The synthetic manufacturer already does it: defect #1 puts backdated entries with `voucher_date` in 2024-02 inside the 2026-01 file, so loading month 33 touches a period that was validated twenty-two months earlier. With the transition wired into the load, every one of those re-uploads hits a refusal on a path where nothing is actually wrong.
corpus/09's only sentence about a period moving backwards is about LOCKED: "If a locked period becomes unreconciled because new data arrived, that is a restatement event with a reason, an owner and a visible delta, not a status flag quietly flipping." That names a mechanism, restatement, and gives it a state, RESTATED. There is no equivalent sentence, and no state, for a VALIDATED or MAPPED period receiving new data. The corpus is explicit at one end of the machine and silent at the other, and the silence is exactly where every ordinary re-upload lands.
Options I can see: (a) skip the transition when a lock row already exists, and record on `LoadResult` that the load touched an already-validated period, so the caller sees it happened rather than the load quietly looking identical to a first upload; (b) re-run the blocking checks and append another `validated` row despite the diagram drawing no self-arrow, treating VALIDATED as re-enterable; (c) treat this as needing a mechanism the corpus does not have -- the VALIDATED/MAPPED analogue of restatement, with a reason and an owner -- in which case neither (a) nor (b) is right and the wiring waits for it.
The cost of (a) is specific and worth stating rather than discovering later: the blocking checks do not re-run. The period keeps the `validated` row it already had, and that row was earned by a check that ran against a different set of facts than the ones now in `fact_gl_entry`. A period can therefore keep serving as "structurally sound" on the strength of a check that never saw the data it is now describing. (b) removes that at the price of a transition section 5 does not draw. (c) removes it properly and blocks the work.
What I will NOT do: pick (b) on my own judgement, or let the load swallow the refusal so a re-upload is indistinguishable from a first upload. Whether a period may re-enter a state is a state-machine question and section 5 is the authority on it; inventing a sixth arrow is not a branch to add in `load_pipeline.py`.
Downstream blocked: nothing is blocked from running. `src/quality/period_state.py`'s refusal is unchanged and still correct in isolation -- this is about what the caller does with it. Until this resolves, the wired transition is the only place in the system where an ordinary second upload of a month meets a hard refusal, and the choice made here decides whether a re-validated period is possible at all.

**Resolved 2026-08-30 by Bhavya Singhal, founder.** Option (a), provisional until the corpus says otherwise. `src/ingest/load_pipeline.load_gl_file` now attempts OPEN -> VALIDATED once per period in `periods_touched`, and where a period_lock row already exists it skips the transition and records a `PeriodTransitionOutcome` (`transitioned=False`) on `LoadResult.period_transitions`, carrying the status it found and the reason. The skip is surfaced on the `/upload` response, not swallowed. The cost stated above is accepted knowingly and is written into the code at the point where it bites (`src/ingest/load_pipeline._validate_loaded_periods`): the blocking checks do not re-run, so an already-validated period keeps a `validated` row earned against facts that have since changed. This is a load-path convenience decision, not a reading of corpus/09 section 5 -- the state machine itself still admits no self-arrow, `validate_period` still refuses, and every test asserting that refusal stands unchanged. If the corpus later states a mechanism, (a) is replaced by it rather than extended.

## OQ-010  Whether a RESTATED period may be read
Raised: 2026-08-31   Task: gating the read paths on corpus/09 section 5's period state machine
Blocked by: corpus gap
What I need: may a period whose current `period_lock` status is `restated` be served on the statement, overview, operating, Ask and pack surfaces?
Options I can see: (a) yes -- RESTATED is a corrected period and the restatement row records the delta, so refusing it hides the corrected number and leaves the superseded one as the last thing anybody saw; (b) no -- the state is entered the moment a change *arrives* (corpus/09 section 5: "a change arrives touching a locked period"), before anyone has explained the delta, so reading it serves a number no human has reviewed; (c) yes for statements but not for a pack, mirroring the MAPPED/RECONCILED split already in section 5.
What I will NOT do: pick one and proceed. `restated` is excluded from both reportable sets in `src/quality/period_gate.py` as the provisional answer, because refusing is the recoverable direction and displaying is not -- CLAUDE.md section 3.2's "make the code fail loudly rather than proceed". This is stated in that module's docstring, in the 422 body and the Ask refusal text, and asserted by `tests/integration/test_period_gate_read_paths.py::test_a_restated_period_is_refused_pending_oq_010`, so answering (a) or (c) changes a named constant and a named test rather than hunting for the behaviour.
Downstream blocked: `src/quality/period_gate.STATEMENTS_REPORTABLE` and `PACK_REPORTABLE`; every read surface that consults them (`src/api/routes/statements.py`, `operating.py`, `overview.py`, `src/reports/pack.py`, `src/semantic/ask_compiler.py`); `src/ingest/repull.py`, which is what will start producing restated periods once the backdated-entry path is wired.

Context that does NOT resolve it: corpus/09 section 5 draws RESTATED as the terminus of "a change arrives touching a locked period" and describes its contents ("new period_lock row, pointer to the prior one, delta explained"), but says nothing about reading it. corpus/09 section 2's check table has "Backdated entry found touching a locked period | INFORMATIONAL, escalating to a restatement event", and INFORMATIONAL does not block output -- which points at (a) but is a statement about the *check*, not about the period state, so it is not the same question. D-040 governs notification, not readability.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (c): a RESTATED period is readable on the statement surfaces and not in a pack, mirroring the MAPPED/RECONCILED split corpus/09 section 5 already draws. Two things decide it. corpus/09 section 2.6 grades the triggering condition ("backdated entry found touching a locked period") INFORMATIONAL, and INFORMATIONAL does not block output -- the escalation noted that this is a statement about the check rather than the state, which is true, but it is the only grading the corpus gives anything on this path and it points one way. And refusing outright has a cost the provisional choice understated: it does not withhold a number, it leaves the *superseded* number as the last thing anybody saw, which is the worse of the two failures. Withholding the pack keeps the property that mattered -- a signed pack still requires a period a human reconciled.
**Not yet applied.** What this changes: `src/quality/period_gate.py`'s `STATEMENTS_REPORTABLE` gains `"restated"` and becomes `("mapped", "reconciled", "locked", "restated")`; `PACK_REPORTABLE` stays `("reconciled", "locked")`. The module docstring's RESTATED paragraph, the 422 body and the Ask refusal text all stop citing OQ-010 as open and state the split instead. `tests/integration/test_period_gate_read_paths.py::test_a_restated_period_is_refused_pending_oq_010` is what changes visibly, as its own docstring anticipated: it becomes two assertions, restated readable on statements and refused for the pack. `src/ingest/repull.py` is still what will produce restated periods, and OQ-018 now owns wiring the check that detects them.

## OQ-011  The named orchestration layer does not exist, and nothing states when anything runs
Raised: 2026-08-31   Task: auditing CLAUDE.md section 6's stated stack against the repository
Blocked by: corpus gap
What I need: what runs on a schedule in this system, how often, and what is deliberately on-demand only?
CLAUDE.md section 6 and corpus/12 section 1's stack table both name "cron plus a Postgres job table" as the orchestration layer, rejecting Airflow, Dagster and Temporal by name and calling cron "genuinely enough for ten tenants." Neither half exists. Repo-wide there are zero matches for cron, celery, apscheduler or pg_cron outside `web/package-lock.json`, where `scheduler` is React's own transitive dependency and unrelated; no migration under `db/migrations/shared` or `db/migrations/tenant` creates a job table; and neither `.github/workflows/ci.yml` nor `deploy.yml` carries a `schedule:` trigger -- CI runs on push and pull_request, deploy on workflow_run. Every path in this system is triggered by an HTTP request or by a developer at a terminal.
No sprint in corpus/12 section 3 carries orchestration as a work item either. It appears in the section 1 stack table and nowhere else, which is why I cannot tell whether it was dropped deliberately -- on-demand is genuinely enough for one synthetic tenant -- or was assumed to arrive with a sprint that never named it.
Then the things that now want it, each checked rather than supposed. corpus/04 section 5 says "Every run re-pulls a trailing window, defaulting to ninety days"; `src/ingest/repull.py` implements the detection and "every run" has no referent -- `query_backdated_entries` is called from exactly one place in `src/`, `src/quality/checks.check_backdated_entries`, which itself has no caller in `src/` at all. That check is the one member of corpus/09's catalogue that genuinely needs a cadence rather than a trigger point, and it is the only part of OQ-018's nine unwired checks that belongs to this escalation; the other eight want wiring, not scheduling, and are OQ-018's subject. D-039 says "the SPEQULA analyst locks when the pack is signed, targeting the 15th of the following month": a monthly cadence, with `POST /periods/{period_key}/lock` as the only way to reach it and nothing in the system that knows what day it is. `src/reports/pack.generate_pack` is reachable only through `POST /reports/generate`, though corpus/08 section 7 calls it the *monthly* management pack. `pytest.ini`'s own marker description says the eval tier is "run nightly/on deploy" and nothing runs nightly. And OQ-005's resolution states outright that a time-based retention policy, if one is ever declared, "is new scheduled-job code" -- it has nowhere to live.
Options I can see: (a) cron plus the job table is still the intent and is simply unbuilt, in which case name the sprint that owns it and its first jobs -- the trailing re-pull is the strongest candidate, since the corpus already says it happens "every run"; (b) on-demand is the deliberate P0 posture, every trigger is a human or an HTTP request, nothing ticks, and the stack table's orchestration row is describing a P1 that should say so; (c) that row is stale in the same way the transforms row is (OQ-012) and the whole stack table needs a pass against what was actually built.
What I will NOT do: invent a schedule. Which jobs run, how often, and in what order is an operational policy with financial consequences -- a re-pull that fires between a pack's assembly and its signature changes what was signed -- and picking a cadence is inventing exactly the kind of unstated operating rule CLAUDE.md section 3.2 exists to prevent. I am also not wiring the eight checks that want a trigger rather than a cadence; those are OQ-018, and `check_backdated_entries` waits on this one.
Downstream blocked: nothing is blocked from running today -- every path has a caller, it is just always a human. What is blocked is everything the corpus describes as recurring: the trailing 90-day re-pull (`src/ingest/repull.py`), which is what will produce the restated periods OQ-010 is about; D-039's monthly lock; the monthly pack's cadence; and the nightly eval run `pytest.ini` already claims happens. See also OQ-014, where this same silence lands on one specific transition.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (b): on-demand is the deliberate P0 posture. Nothing in P0 ticks; every trigger is a human or an HTTP request.
**A correction to this escalation, which was wrong.** It claimed corpus/04 section 5's "Every run re-pulls a trailing window, defaulting to ninety days" has no referent. It has one, in the same section: section 5 is titled "Load pipeline, and what it guarantees", and its own pipeline diagram lists `load_run` as a step ("one run, one id, wrapping everything below"), which is `app.load_run`, whose table comment grains it as "one ingestion execution." "Every run" means every load run. So the trailing re-pull is not a scheduled job at all -- `check_backdated_entries` wants wiring at the end of each GL load, alongside the other load-time checks, and it moves out of this escalation into OQ-018 with the rest. With that correction there is no remaining P0 job that needs a cadence: D-039's "targeting the 15th" describes when an analyst acts, not a timer; the pack is generated when someone asks for it; and OQ-005's retention policy is request-triggered by D-068.
**Not yet applied.** What this changes: no orchestration code is written, and no job table migration is added. CLAUDE.md section 6's orchestration line and corpus/12 section 1's Orchestration row are corrected to say cron plus a Postgres job table is P1, with the trigger stated, rather than describing a P0 layer that does not exist. `src/quality/checks.check_backdated_entries` is wired in `src/ingest/load_pipeline.load_gl_file` under OQ-018. `pytest.ini`'s marker description drops "run nightly/on deploy" for "run on every commit in CI" (see OQ-013), since that is what `.github/workflows/ci.yml` actually does and no nightly run exists or is now planned.

## OQ-012  dbt is named as the transform layer and no dbt project exists
Raised: 2026-08-31   Task: auditing CLAUDE.md section 6's stated stack against the repository
Blocked by: conflict between CLAUDE.md sections 6 and 7 and corpus/12 section 1 on one side, and the repository on the other
What I need: is dbt still the intended transform layer, or did hand-written Python-side SQL become the decision without anyone recording that it had been made?
CLAUDE.md section 6 names "dbt-core for transforms" and section 7 lists `/dbt   transforms` in the repo layout. corpus/12 section 1's stack table names "dbt-core on Postgres," gives the reason ("Tests, docs and lineage come free"), and lists "Hand-written SQL scripts" in its Rejected column. There is no `/dbt` directory, no `dbt-core` in `requirements.txt` or `requirements-dev.txt`, no `dbt_project.yml`, no model, no profile, and no reference to dbt anywhere in the repository outside those four lines of documentation and `ARCHITECTURE_MAP.md`, which already notes the absence without escalating it. Every transform is Python-side, across 32 modules that build SQL as f-strings over `psycopg` cursors -- `src/ingest/canonical.py`, `src/reports/*`, `src/semantic/compiler.py` and `src/quality/*` among them. That is precisely the option the stack table's Rejected column names.
The same table row family carries a second, smaller drift that belongs with this one rather than in an escalation of its own: CLAUDE.md section 6 also names "SQLAlchemy Core (not the ORM for analytical paths)", and `sqlalchemy` appears nowhere in the repository -- not in either requirements file, not in an import. Both the transform layer and the SQL-construction layer named in section 6 are absent, which is why the question is about the stack table rather than about dbt alone.
What makes this more than bookkeeping is the reason given for choosing dbt: "tests, docs and lineage come free." Lineage is not incidental here. CLAUDE.md invariant 7 requires every displayed number to carry a citation resolving to source rows, and `src/semantic/citation.py` builds that by hand, per metric, on two code paths (see OQ-016 for the surfaces where it is not built at all). If dbt was meant to be the lineage substrate that invariant leans on, its absence is load-bearing rather than cosmetic.
Options I can see: (a) dbt was dropped deliberately -- one Postgres, one language, and the semantic compiler already emits SQL from metric contracts, so a second SQL-generating system would be a competing source of truth against CLAUDE.md invariant 2 -- in which case sections 6 and 7 and corpus/12's transforms row should say so, and the reason recorded as a decision; (b) dbt is still intended and unbuilt, in which case which sprint owns it and what its first models are; (c) a split: dbt for the staging-to-canonical transforms in `src/ingest/`, hand-written SQL for the metric compiler, which is the division the corpus's own layering suggests but nowhere states.
What I will NOT do: create a `/dbt` directory to satisfy the documentation, or edit CLAUDE.md sections 6 and 7 to describe what was built instead. Either resolves the question by fiat, and the second is the one that looks harmless and is not -- it converts an undeclared decision into a settled one with no record that anybody made it, which is CLAUDE.md section 4's "a commit that silently resolves an open decision is a defect, even if the code works."
Downstream blocked: nothing is blocked from running -- the transforms work. What is unresolved is whether a new transform belongs in Python or in a dbt model, a question every future ingestion or statement change now has to answer with no authority to answer it from. `/dbt` in CLAUDE.md section 7's tree is currently a directory a reader will look for and not find, and the same is true of SQLAlchemy in section 6.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (a): dbt is dropped deliberately, and so is SQLAlchemy. The reason is the one the escalation put first: `src/semantic/compiler.py` already emits SQL from metric contracts, and a second SQL-generating system would be a competing source of truth for how a number is computed, which is what CLAUDE.md invariant 2 exists to prevent. The lineage argument for dbt is answered the same way -- lineage in this system runs through `src/semantic/citation.py` and the metric registry, per corpus/07 section 8, not through a transform graph.
**Not yet applied.** What this changes: no code. CLAUDE.md section 6's "dbt-core for transforms" and "SQLAlchemy Core (not the ORM for analytical paths)" are replaced with what is built -- hand-written SQL over `psycopg`, with the compiler as the only generator of metric SQL -- and section 7's repo layout drops the `/dbt` line. corpus/12 section 1's Transforms row is corrected: the Choice column becomes hand-written SQL on Postgres, and the reason records that dbt was rejected for competing with the metric compiler rather than on cost. This is a documentation correction that records a decision, not a silent one: the four stale lines are what made it look unresolved.

## OQ-013  `pytest -m eval` runs three of the seven eval files, and CI does not
Raised: 2026-08-31   Task: auditing CLAUDE.md section 9's gating commands against what they collect
Blocked by: corpus gap / conflict between CLAUDE.md section 9 and the marker as actually applied
What I need: is `@pytest.mark.eval` meant to mean "the corpus/11 gating tier," in which case four files are missing it, or does it mean something narrower that those four are deliberately outside?
CLAUDE.md section 9 says "`pytest -m eval` runs the golden set. A regression is a build break, not a ticket." `pytest.ini` defines the marker as "gating evaluation-tier tests (corpus/11 section 3) -- slow, need a live Postgres, run nightly/on deploy."
Measured: `pytest tests/eval --collect-only` finds 19 tests across 7 files. `pytest tests/eval -m eval --collect-only` finds 4, across 3: `test_statements_tie_consumer.py`, `test_statements_tie_manufacturer.py` and `test_trial_balance_36_months.py`. Fifteen tests in four files carry no marker and are silently deselected. `test_golden_questions.py` (4 tests) is corpus/10's golden set -- the literal thing section 9's sentence names -- and `test_37_of_37_gating_questions_behave_correctly` and `test_14_of_14_refusals_refuse` are the two corpus/11 calls gating. `test_monthly_pack.py` (5 tests) includes `test_rerender_is_byte_identical_even_after_the_underlying_data_changes`, corpus/08's re-render guarantee. `test_sprint3_acceptance.py` (2) and `test_sprint6_acceptance.py` (4) are the acceptance criteria corpus/12 states for those sprints, which is what CLAUDE.md section 11's definition of done points at. So the marker selects the statement tie-outs and excludes the golden questions.
CI being unaffected is the sharp edge, not the mitigation. `.github/workflows/ci.yml`'s gating step is `pytest tests/eval -q`, by path, so it collects all 19. The two commands have silently diverged and the one written into CLAUDE.md is the weaker one: anyone who runs `pytest -m eval` locally before pushing, exactly as section 9 instructs, gets a green that CI does not owe them, having run four tests and reported success in the same words it would use for nineteen.
Options I can see: (a) the four files should carry `pytestmark = pytest.mark.eval` and the marker means what `pytest.ini` says -- the likely answer, and the reason I am not simply applying it is below; (b) the marker is narrower than the directory on purpose (the three marked files are the ones needing a fully loaded synthetic dataset, the other four need something else) and CLAUDE.md section 9 should name the path rather than the marker; (c) drop the marker and address `tests/eval` by path everywhere, since the directory is already the selector and a second one that disagrees with it is what produced this.
What I will NOT do: add the marker to four files. It looks like a four-line change and it is not a neutral one -- it makes fifteen tests gating for everyone running the documented command, and I have not run them (they need a live Postgres). At least one is coupled to a still-open escalation: `test_golden_questions.py`'s `EXPECTED_STATUS` table holds "Where did the cash go last month?" at `blocked` for OQ-004's reason, so resolving OQ-004 moves that file. Which tests gate is corpus/11 section 3's call, not a marker's.
Downstream blocked: nothing is blocked from running. What is affected is what a green local run means: `pytest -m eval` currently cannot regress on the golden questions, the monthly pack, or either sprint acceptance suite. Until this resolves, `pytest tests/eval` is the command that does what CLAUDE.md section 9 describes, and `pytest -m eval` is not.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (a): the marker means what `pytest.ini` says, and the four unmarked files gain it. The escalation's stated reason for not applying it was that doing so would make fifteen tests newly gating; that reason does not hold, because `.github/workflows/ci.yml` runs `pytest tests/eval -q` by path and those fifteen already gate on every commit. Adding the marker changes nothing about what breaks a build. It only makes the command CLAUDE.md section 9 documents select what CI already selects.
**Not yet applied.** What this changes: `pytestmark = pytest.mark.eval` at module level in `tests/eval/test_golden_questions.py`, `test_monthly_pack.py`, `test_sprint3_acceptance.py` and `test_sprint6_acceptance.py`, after which `pytest -m eval` collects 19 tests across 7 files rather than 4 across 3. `pytest.ini`'s marker description also loses "run nightly/on deploy" per OQ-011 -- there is no nightly run. No test body changes and no assertion changes; anything currently failing in those four files was already failing in CI and is unaffected by this.

## OQ-014  Nothing in `src/` can produce the books-to-bank run that MAPPED -> RECONCILED requires
Raised: 2026-08-31   Task: auditing the period state machine's reachable transitions, re-checked against `src/api/routes/periods.py`
Blocked by: corpus gap
What I need: what triggers a books-to-bank reconciliation run, and when?
`src/quality/period_state.reconcile_period` refuses MAPPED -> RECONCILED with "no books-to-bank reconciliation_run exists for {period} -- run src/quality/books_to_bank.run_books_to_bank first". `run_books_to_bank` has zero callers in `src/`: the only three references a grep finds are that refusal string, the module's own docstring, and the definition. Every actual call is in a test -- `tests/helpers.py`, `tests/unit/test_books_to_bank.py`, `tests/integration/test_period_state_machine.py`, `tests/integration/test_period_transitions_wired.py`, `tests/eval/test_sprint3_acceptance.py`.
Checked again after the periods route landed, since that is what should have closed it: it did not, and the reason is narrower than "no endpoint." `POST /periods/{period_key}/reconcile` exists and is right in what it does -- `reconcile_period_from_current_checks` deliberately refuses to take the period's condition from the caller, runs `check_trial_balance` itself, and finds the run through `latest_reconciliation_run_id(..., "books_to_bank")`. It *finds* a run. It does not *produce* one. The only writer of a `reconciliation_run` row anywhere in `src/` is `src/ingest/load_pipeline.py:258`, and it writes `check_type="trial_balance"` -- a different row, which that lookup correctly does not match. (The module is not orphaned, which is what makes this easy to miss: `period_state.py`, `load_pipeline.py` and `trial_balance.py` all import from it. It is the one function that computes the reconciliation that nothing calls.)
So in a deployment driven only through the API, `POST /periods/{key}/reconcile` returns 422 for every period, permanently. No period reaches RECONCILED; and because `src/quality/period_gate.py` gates the read paths on those states, the surfaces requiring RECONCILED never serve. The integration and eval suites pass because they call `run_books_to_bank` directly to set up their fixtures, which is exactly the shape of gap that stays invisible until a pilot.
The corpus does not say when the reconciliation runs. corpus/09 section 3.2 describes the mechanism in full, section 2.5 lists it as one of P0's two reconciliation checks, and corpus/04 grains `reconciliation_run` as "result of each reconciliation check per period." None of that is a trigger. It is OQ-011's silence arriving at one specific place, with a harder consequence.
Options I can see: (a) it runs at the end of a bank-statement load, symmetric with the trial balance check running at the end of a GL load, on the reasoning that the bank file is what changes the answer; (b) it runs inside `reconcile_period_from_current_checks` itself, immediately before the lookup, so asking to reconcile computes the reconciliation -- simple, but then the residual a named human is recording that they looked at is computed by the same request that records they looked at it, which is not obviously what corpus/09 section 5 means by "books to bank within tolerance"; (c) it is a scheduled job, i.e. OQ-011's missing layer; (d) it is its own human action with its own endpoint, separate from the transition, mirroring D-052's deliberate blankness -- the tolerance is unset because a human decides, so perhaps running it is a human's act too.
What I will NOT do: pick a trigger and wire it. The difference between (a), (b) and (d) is a difference in what a reconciled period *means* -- computed at load, computed on request, or computed by a named person -- and corpus/09 section 5 is the authority on what that transition asserts. Nor will I loosen the refusal to accept the existing `trial_balance` run in its place: the two check the ledger against different things, and D-052's blankness is about the books-to-bank residual specifically.
Downstream blocked: `POST /periods/{period_key}/reconcile` (`src/api/routes/periods.py:48`) cannot succeed through any API-driven path, and `POST /periods/{period_key}/lock` behind it, RECONCILED being LOCKED's only predecessor. Behind those: every read surface `src/quality/period_gate.py` gates on RECONCILED or LOCKED, and `src/reports/pack.py`, since a pack is signed against a locked period per D-039. The eval and integration suites are unaffected, because they reach the state by calling the function directly -- which is why this has never surfaced as a test failure.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (d): the books-to-bank run gets its own endpoint, `POST /periods/{period_key}/reconciliation-run`, separate from the transition that consumes it.
**Explicitly not option (a), at load.** `fetch_books_total` takes a `mapping_version_id` and resolves class movements through it. At first onboarding there is no approved mapping version, so a load-time run would compute against the version-0 placeholder (`src/ingest/canonical.get_placeholder_mapping_version`, seeded only so `fact_gl_entry.mapping_version_id` NOT NULL has a target and carrying no `map_account` rows). `class_movements` over it returns nothing, `books_total` is zero, and the recorded residual is just the negative of the bank total -- a stored number that looks like a reconciliation and is an artefact of running too early. Not option (b) either: corpus/09 section 3.2 requires the residual to be stated, itemised against the seven modelled difference categories, and *seen* before anyone decides -- so computing it has to precede the transition, not accompany the request that records the decision.
**Not yet applied.** What this changes: a new endpoint in `src/api/routes/periods.py` that calls `src/quality/books_to_bank.run_books_to_bank` and `write_reconciliation_run` with `check_type="books_to_bank"`, resolving the mapping version the way `reconcile_period_from_current_checks` already does -- off the period's own lock row -- and returning the residual and its modelled differences so the caller sees what they will be reconciling against. `reconcile_period` and `latest_reconciliation_run_id` are unchanged; the refusal text stops naming a function and names the endpoint. `run_books_to_bank` gains its first caller in `src/`, which is what closes this. D-052 stays blank and is untouched: this decides when the reconciliation is *run*, not what residual is acceptable.

## OQ-015  "Contributing the largest imbalance" is undefined, and the check ranks something else
Raised: 2026-08-31   Task: auditing `src/quality/trial_balance.py` against corpus/09 section 3.1
Blocked by: corpus gap
What I need: what does "contributing" mean in corpus/09 section 3.1's "the failure names the accounts contributing the largest imbalance"?
`evaluate_balances` sorts `account_balances` by `-abs(net_amount)` and takes the top five. In a trial balance every account carries a balance, and the balances sum to zero when the period is sound -- so absolute net balance ranks the period's *biggest accounts*, which is a property of the company's size, not of the failure. The one-sided voucher that broke the period may sit on a Rs 40,000 ledger and never appear in the five. On the synthetic manufacturer the list would be the major sales and expense ledgers in every failing month, whatever the actual cause, because they are the largest accounts in every month.
The module is already honest about this and I am not reporting a hidden defect: `imbalance_exception`'s docstring says in as many words that the ranking "surfaces the period's biggest accounts, which need not include the one-sided voucher that caused the imbalance," and the text it emits reads "Accounts carrying the largest balances in the period", not "contributing". So nothing displayed overclaims. What remains wrong is that the field is named `largest_contributors`, corpus/09 section 3.1 asks for contributors, and the check does not compute them -- a caller reading the field name gets the wrong idea, and the corpus requirement is not met by the code that cites it.
The reason this is an escalation rather than a fix: "contributing" has no reading I can defensibly pick. A trial balance imbalance is a single scalar with no per-account attribution -- the sum is non-zero, and no account is individually responsible for it in any way the data supports. Every candidate definition is a heuristic about cause, and choosing one is choosing what the system asserts caused a blocking failure.
Options I can see: (a) accounts whose net balance falls on the same side as the imbalance's sign, ranked by magnitude -- narrows the list to accounts that could be part of it, still not attribution; (b) accounts touched by vouchers that do not themselves balance, which is attribution in the real sense, since a period nets to non-zero only because some voucher does: `fact_gl_entry` carries `voucher_no`, so this is computable -- group by voucher, keep the ones whose lines do not sum to zero, name their accounts. Strictly more work, strictly more useful, and it would find the Rs 40,000 ledger; (c) accounts whose balance moved most against the prior period, which catches a new break but misfires on genuine business movement; (d) "contributing" was loose drafting for "largest", the current behaviour is what was meant, and corpus/09 section 3.1's wording is what should change.
What I will NOT do: change the ranking. Which accounts a blocking failure names is what a human is handed in order to go and fix a period that cannot produce a statement, and pointing them at the wrong five is worse than today's behaviour, which at least does not claim causation. (b) is the option I would argue for, and it is a real behaviour change to a blocking exception's contents, not a rename.
Downstream blocked: nothing is blocked from running -- the check itself is correct, `blocking` fires on any non-zero total at D-051's zero tolerance, and statement assembly stops as it should. What is affected is the exception queue's usefulness on the check corpus/09 calls "the simplest and most important check in the system": `TrialBalanceCheckResult.largest_contributors`, its rendering in `imbalance_exception`'s description, and whatever the exceptions screen puts in front of an analyst. The field name should move with the resolution -- under (d) it becomes `largest_accounts`; under (a) or (b) the name is right and the computation is what changes.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (b): attribute the imbalance to unbalanced vouchers. Group `fact_gl_entry` by `voucher_no` for the period, keep the vouchers whose lines do not sum to zero, and name the accounts those vouchers touch. This is attribution in the real sense rather than a proxy for it -- a period nets to non-zero only because some voucher does -- and it is computable from data already held.
**Not yet applied.** What this changes: `evaluate_balances` in `src/quality/trial_balance.py` no longer produces `largest_contributors` by sorting on absolute net balance; the ranking becomes a voucher-level query, which means it stops being derivable from the `{(account_code, account_name): net_amount}` mapping the pure function takes today and needs either a wider input or a separate DB-backed function beside `check_trial_balance`. `AccountContribution` keeps its shape but is populated from the offending vouchers, and the field name `largest_contributors` becomes accurate rather than misleading. `imbalance_exception`'s description changes from "Accounts carrying the largest balances in the period" to naming the unbalanced vouchers and their accounts, and its docstring loses the paragraph disclosing that the ranking is a proxy. The blocking behaviour is untouched -- D-051's zero tolerance and `blocking=not balanced` are unchanged; only what the failure *names* changes.

## OQ-016  Five Ask intents and every statement surface return numbers with no citation
Raised: 2026-08-31   Task: auditing the read surfaces against CLAUDE.md invariant 7 and corpus/07 section 8
Blocked by: conflict between CLAUDE.md invariant 7 / corpus/07 section 8 and what the read surfaces do
What I need: does "every displayed number carries a citation that resolves to source rows" apply to a statement line, a trend point and a bridge component -- and if it does, what does that citation resolve to when the number is not a single compiled metric?
`build_citation` is called from exactly two places in `src/`: `src/api/routes/overview.py:55` and `src/semantic/ask.py:195`. `GET /overview/tiles` cites every tile and refuses to display an uncitable one, which is the invariant working. Inside `POST /ask` the citation is built only when `result.compiled_metric is not None`, and that field is set by `_metric_value`, by `_metric_comparison` (for the current period only -- the comparison cell's number is uncited), and by `_metric_breakdown` / `_metric_ranking`, which delegate to `_metric_value`. It is not set by `_metric_trend`, which returns a per-month `series` of values, every one `citation: null`; nor by `_statement_view`, which returns a full P&L, its subtotals and the balance sheet's balances flag, every line uncited; nor by `_variance_explain`, which returns a bridge plus current and prior values, where invariant 15 requires the components to sum to the total and none of them resolves to rows; nor by `_data_health`, which returns `residual_inr` per reconciliation run and `unmapped_value_inr` -- the module argues this intent reports the period's state rather than reporting through it, which is fair for a status string and is where I stop being sure for a rupee residual computed from facts; nor by `_definition_lookup`, which returns the registry contract and is the one of the five that is genuinely not a number computed from facts, included only for completeness.
The statement routes are the larger surface and none of them touches citations at all: `citation` does not appear once in `statements.py`, `operating.py`, `reports.py`, `data_health.py`, `exceptions.py` or `forecast.py`. `GET /statements/pnl` returns `lines` and `subtotals` as rupee strings and its response shape has no citation field to populate.
What makes this a question rather than a defect list: a citation as `src/semantic/citation.py` builds it is a per-metric object -- a metric id, a value, a query hash, the rows behind it. A P&L line is not a metric in the registry; it is a statement line assembled from canonical classes by `src/reports/statement_lines.py`. A bridge component is a difference of two metrics across two periods. A trend is twelve metrics. Each needs a citation shape that does not exist, and corpus/07 section 8 defines the object for a metric answer. So invariant 7 plainly applies to these numbers and the mechanism for satisfying it on these surfaces is unspecified.
Options I can see: (a) invariant 7 is metric-scoped and the statement surfaces satisfy provenance differently -- a statement line's provenance is its canonical-class mapping plus the mapping version, which those responses already carry as `mapping_version_id`, and corpus/07 section 8's citation object was written for the Ask answer specifically; (b) it applies literally to every displayed number and each surface needs its own citation shape -- per-line for a statement, per-point for a trend, per-component for a bridge -- which is real work in corpus/07 section 8 before any of it is code; (c) between the two: trends and comparisons cite, since they are metrics and `build_citation` already works on each, while statements and bridges get a documented, deliberately different provenance object.
What I will NOT do: attach a citation to a statement line by inventing what it resolves to, or invoke (a) on my own reading to declare six surfaces compliant. Invariant 7's teeth are that an uncitable number is not shown at all -- "not badged, not greyed out. Not displayed" -- so reading it literally today blanks the P&L, the balance sheet, every trend and every bridge, and reading it narrowly excuses six surfaces at once. Both are decisions about what the product shows, and both should be made rather than inferred by whoever next edits a route.
Downstream blocked: nothing is blocked from running, which is the concern -- these surfaces serve today. Affected: `src/semantic/ask_compiler.py`'s `_metric_trend`, `_statement_view`, `_variance_explain`, `_data_health` and `_definition_lookup`, plus the comparison half of `_metric_comparison`; and the whole of `src/api/routes/statements.py`, `operating.py` and `reports.py`. `tests/eval/test_sprint3_acceptance.py::test_every_overview_tile_either_resolves_with_a_full_citation_or_carries_no_citation_at_all` asserts the invariant for the tiles and there is no equivalent for anything else, which is part of why this did not surface as a failure -- and that test is itself one of the fifteen OQ-013 shows `pytest -m eval` does not collect.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (c), with the split stated plainly: trends and comparisons cite per point, because each point is a compiled metric and `build_citation` already works on one; statements get a separate, documented provenance object, because a statement line is a canonical-class rollup assembled by `src/reports/statement_lines.py` and not a registry metric, and the statement responses already carry the `mapping_version_id` that determines it; bridges cite their endpoints, since a bridge component is a difference between two cited metrics and the endpoints are what resolve to rows.
**This is an interpretation, not something corpus/07 section 8 states.** Section 8 defines the citation object for a metric answer and does not say what a statement line or a bridge component carries; CLAUDE.md invariant 7 says every displayed number carries a citation and does not distinguish between them. Reading invariant 7 as satisfied by a different provenance object for statements is a choice made here, and it should be written into corpus/07 section 8 as one -- if it stays only in code, the next reader finds the same conflict this escalation found.
**Not yet applied.** What this changes: `_metric_trend` and `_metric_comparison` in `src/semantic/ask_compiler.py` carry a `CompiledMetric` per point (including the comparison cell, which is uncited today) so `src/semantic/ask.py` can build a citation for each; `_variance_explain` carries the two endpoint metrics; `src/semantic/citation.py` gains a statement-provenance object distinct from `Citation`, which `src/api/routes/statements.py`, `operating.py` and `reports.py` attach to their line output. `_data_health` and `_definition_lookup` are resolved as out of scope for invariant 7 and say so in their docstrings -- one reports a period's state rather than a number computed through it, the other returns a contract. corpus/07 section 8 gains the statement and bridge cases. `tests/eval/test_sprint3_acceptance.py`'s tile assertion gains siblings for the statement and trend surfaces; without those this resolution is unenforced.

## OQ-017  The golden set runs against month 24 of 36 and calls it the last month
Raised: 2026-08-31   Task: correcting `tests/fixtures/golden_ir.py`'s date comment
Blocked by: corpus gap
What I need: should corpus/10's golden questions resolve "last month" to the last month of the synthetic manufacturer's GL data, or is a mid-series month the deliberate choice?
`tests/fixtures/golden_ir.py` sets `LAST_MONTH = "2025-03"` and described it as the last month of GL data in a manufacturer running "36 months from 2022-04". Both figures were wrong: `synthetic/manufacturer/profile.py` gives `FISCAL_START = date(2023, 4, 1)` and `N_MONTHS = 36`, so the company runs 2023-04 through 2026-03 and 2025-03 is month 24 of 36. I corrected the comment in the commit that raised this. I did not move the fixtures, because which month the golden set exercises is a corpus/10 and corpus/11 question, not a comment.
Why it may matter: every "last month" question in corpus/10 -- across `metric_value`, `metric_trend`, `metric_comparison`, `statement_view` and `variance_explain` -- currently resolves to a month with twelve months of data on either side of it, so nothing in the gating suite asks a question at either end of the dataset. The ends are where the interesting failures are. A `metric_comparison` on the first month has no prior period. A `metric_trend` window running past 2026-03 reaches months with no rows, which is exactly the `NotCitable` "requires data not held" path `src/semantic/ask.py` documents as "an ordinary question, not a corner case." And 2026-03 is a fiscal year end. A mid-series month exercises none of it.
It may equally not matter, and there is a real argument for the month as it stands: a month with full history on both sides is the one where the largest number of intents resolve to an actual value rather than a refusal, which is what a *gating* accuracy suite wants -- corpus/11 section 3.1's tier is about numbers being exactly right, and boundary refusals are a different tier's job.
Options I can see: (a) move `LAST_MONTH` to 2026-03 and `PRIOR_MONTH` to 2026-02 so the constants mean what they say, which re-baselines every expected value in the file and is therefore a corpus/11 exercise, since CLAUDE.md section 9 forbids recording current output as the expected value; (b) leave the fixtures and rename the constants to what they are (`REFERENCE_MONTH`), adding boundary questions as their own cases rather than by moving these; (c) both -- keep the mid-series month for the 37 gating questions and add a small boundary set alongside corpus/10's 14 refusals, on the grounds that a question about a month with no data is a refusal question.
What I will NOT do: move the fixture months. Every expected value in `GATING_FIXTURES` is tied to the month it names, so re-pointing the two constants silently re-baselines the gating suite against a different period -- a two-line edit that changes what 37 gating questions assert.
Downstream blocked: nothing. `tests/eval/test_golden_questions.py` passes or fails on its own terms either way, and it is one of the four files `pytest -m eval` does not collect (OQ-013). What is unresolved is whether the gating suite has ever exercised a data boundary; on the current fixtures it has not.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (c): keep the mid-series month for the 37 gating questions, and add boundary cases alongside corpus/10's 14 refusals. The gating tier stays a test of whether numbers are exactly right, which is what corpus/11 section 3.1 makes it; the boundary behaviour is refusal behaviour and belongs with the refusals.
**Not yet applied.** What this changes: `LAST_MONTH` and `PRIOR_MONTH` in `tests/fixtures/golden_ir.py` do not move, so no expected value in `GATING_FIXTURES` is re-baselined. The constants are renamed to say what they are rather than what they are not -- the corrected comment already records that 2025-03 is month 24 of 36. New refusal fixtures cover a `metric_comparison` on 2023-04 (no prior period), a `metric_trend` window running past 2026-03 (months with no rows, the `NotCitable` "requires data not held" path), and a `metric_value` on a month outside the dataset entirely. Their expected outcomes are derived from corpus/07 section 6's refusal taxonomy, not recorded from what the system currently returns, per CLAUDE.md section 9.

## OQ-018  Nine of the eleven checks in the catalogue have no caller, and corpus/09 does not say where three of them fire
Raised: 2026-08-31   Task: splitting the unwired check catalogue out of OQ-011, where it was first recorded as evidence
Blocked by: corpus gap
What I need: for each catalogue row in corpus/09 section 2, where does the check fire -- at load, at statement assembly, at pack generation, or on a schedule? corpus/09 gives every row a severity and an action; it gives most of them no trigger.
Nine of the eleven check functions in `src/quality/checks.py` have zero callers anywhere in `src/`. The two that run are `check_mixed_uom`, called from `src/api/routes/operating.py:89`, and `fetch_freshness`, called from `data_health.py`, `admin.py` and `src/reports/pack.py` -- and both run for the same reason, which is worth stating because it is the shape of the whole problem: a read surface happened to need the answer for display. Neither is called because the catalogue says it should run. There is no path in this system that runs the catalogue. `write_exceptions` is called from exactly one place, `src/ingest/load_pipeline.py:262`, for the trial balance imbalance, so the `exception` table has exactly one producer.
Where each of the nine would fire if it fired, read off corpus/09 section 2 rather than chosen by me:
1. `check_zero_gl_rows` -- **statement assembly**, or any read path that takes a range. corpus/09 section 2.1: "A month in the requested range has zero GL rows", BLOCKING, "Period not reportable". "The requested range" is a read-time phrase; at load there is no requested range. The corpus names this one.
2. `check_stream_supplied` -- **the data health screen**, named in the Action column ("A required stream has not been supplied at all", BLOCKING, "Named on the data health screen"), so read time on `GET /data-health`. The surface is named and the input is not: nothing in corpus/09 says which streams are *required* for a given company, and the function takes `stream` as a parameter precisely because it cannot know. That half is a gap, and possibly one corpus/01's data request pack answers -- corpus/09 does not cite it, so I am not asserting that it does.
3. `check_unmapped_value` -- **corpus/09 does not say.** Section 2.1's action is "Unmapped rupee value displayed", which is a surface, not a trigger. Candidates are the mapping pass, the freeze, and every statement assembly, and they are not equivalent, because D-053's bands are on *period* value and a mapping version spans periods.
4. `check_opening_balances_present` -- **balance sheet assembly**, named in the Action column ("Opening balances missing for the earliest period", BLOCKING, "Balance sheet cannot be assembled"). The corpus names this one.
5. `check_duplicate_file_upload` -- **at load**. corpus/09 section 2.3's action is "Content hash catches it. Idempotent by design, and a weekly event", and a content hash is computed when a file arrives. "A weekly event" describes how often the situation occurs, not a cadence for the check.
6. `check_duplicate_content_different_voucher` -- **at load**. corpus/09 section 2.3's row is "Duplicate voucher number and line within a period", BLOCKING, "Deduplicated on `row_hash`, difference logged", and row-hash dedup is a load operation. The function deliberately widens the row from id to content, because corpus/11 section 2.2's defect #2 is a re-export under new voucher numbers that `row_hash` dedup structurally cannot catch, and its docstring says so. That widening changes what is detected, not when.
7. `check_new_ledger_mid_year` -- **corpus/09 does not say.** Section 2.7's row is "A new account appears carrying large value", WARNING, "Queued by rupee value"; the queue is named, the trigger is not. Load and the mapping pass are both plausible, since corpus/06 section 4.4 treats a new ledger as a mapping event. Two things make this one unwireable as written rather than merely unwired: "large value" is an undeclared threshold of the OQ-001 and OQ-003 family and the function has none, so it flags every first-appearance account; and because first appearance is `NOT EXISTS` over prior periods, a company's earliest period makes every account new, so wiring this at load would emit one WARNING per account on the first month ingested. The corpus's own phrase is "mid-year", which implies not the first period, but nothing states that as a rule.
8. `check_backdated_entries` -- **on a schedule**, and it is the only one of the nine that belongs to OQ-011 rather than here. corpus/09 section 2.6 gives the row ("Backdated entry found touching a locked period", INFORMATIONAL escalating to a restatement event, "Per section 5") and corpus/04 section 5 gives the cadence ("Every run re-pulls a trailing window, defaulting to ninety days"). A trailing window is recurring by construction, not per-upload. Separately, the function returns every backdated entry in the window rather than only those touching a locked period, leaving the restatement decision to `src/quality/period_state.restate_period` -- so corpus/09's "touching a locked period" qualifier is not applied at this layer. A scope question for whoever wires it, noted rather than changed.
9. `check_margin_sign_flip` -- **corpus/09 does not say.** Section 2.7's row is "Margin sign flip on a product or channel", WARNING, "Queued". It needs a current and a prior period over `fact_channel_order_line`; the consumer sales load, statement assembly and pack generation are all plausible, and corpus/08's pack carries a margin analysis section (`src/reports/pack.build_margin_analysis`) that is the most natural home. One correction to the module's own docstring while I am here, not made in code this session: its scope-boundary paragraph says corpus/11 defect #9 lands in an un-ingested stream and is "structurally undetectable", which was true when written and is now stale -- `src/ingest/canonical.py:582` writes `fact_channel_order_line`, so the data this check needs exists.
So, of the nine: three (1, 2, 4) fire where corpus/09's own Action column names the surface; two (5, 6) are inferable from the mechanism the corpus describes rather than stated outright; one (8) is a cadence corpus/04 section 5 gives and is already open as OQ-011; and **three -- `check_unmapped_value`, `check_new_ledger_mid_year`, `check_margin_sign_flip` -- have no trigger stated anywhere.** That is the gap this escalation is about. corpus/09 is written as a catalogue of what is true of the data, with a severity and a consequence per row; it is not written as a specification of when anything runs.
What it means today. corpus/09 section 1's governing rule is "the system never silently continues when critical data is unreliable," and nine of the eleven mechanisms for detecting that it is unreliable are inert. Three consequences are concrete rather than theoretical. First, three paths read the exception queue as a gate -- `src/ingest/load_pipeline.py:190` computes `blocking_exception_count`, which the period state machine consults; `src/api/routes/reports.py:190` serves a pack's blocking exceptions; and `src/reports/signoff.py:164` is corpus/08 section 10's sign-off gate, the one `tests/eval/test_monthly_pack.py::test_cannot_sign_with_open_blocking_exception_unless_overridden` asserts. All three are real gates and all three read a table with one producer, so the only condition that can ever stop a pack being signed is a trial balance imbalance. Second, D-053 is a *resolved* decision with declared numbers ("block above 2% of period value, badge between 0.5% and 2%") and those numbers are enforced nowhere: `UNMAPPED_BLOCK_THRESHOLD_PCT` and `UNMAPPED_WARN_THRESHOLD_PCT` are referenced only inside the uncalled `check_unmapped_value`. `src/mapping/review.freeze_mapping_version` enforces a version-wide 98% coverage gate at freeze (`COVERAGE_THRESHOLD`, corpus/06a's Coverage tab), which is arithmetically the same 2% at one moment but is neither per-period nor recurring; `src/quality/period_gate.py` reports the unmapped rupee value on the refusing path per corpus/07 section 6 but does not band on it. The 0.5-2% WARNING band therefore never fires, and nothing in this system is ever badged for unmapped value. Third, three of the nine are BLOCKING (`check_zero_gl_rows`, `check_stream_supplied`, `check_opening_balances_present`), so a period with no GL rows at all, a missing stream, or no opening balances produces no exception and nothing halts -- the conditions corpus/09 section 2.1 says must stop output.
For completeness on the count: a twelfth catalogue member, the pure helper `cash_flow_ties_to_balance_sheet` (corpus/09 section 2.4, one of the four bold checks), also has no caller in `src/`. It is excluded from the nine because it is blocked upstream for a different and already-escalated reason -- OQ-004 leaves six cash flow leaf metrics with no formula, so the cash flow statement does not assemble and there is no closing cash to tie. Wiring it is downstream of OQ-004, not of this.
Options I can see: (a) wire each check at the trigger point corpus/09's Action column names, and treat the three with no stated trigger as their own smaller escalations -- the most faithful to the corpus and the one that leaves three checks still inert; (b) one catalogue runner, `run_check_catalogue(period_key)`, invoked at the end of a load and again at statement assembly, which is closer to how corpus/09 reads as a document -- a catalogue with severities rather than nine independent hooks -- at the cost of running checks against data they do not concern and of picking those two invocation points without the corpus naming them; (c) wire the three BLOCKING ones now, on the grounds that corpus/09 section 1's governing rule is specifically about not continuing past unreliable data and the WARNING and INFORMATIONAL rows block nothing, deferring the rest until a queue surface reads them.
What I will NOT do: choose firing points for the three the corpus does not name, supply the "large value" threshold `check_new_ledger_mid_year` needs, or wire even the three BLOCKING ones on my own reading. A check wired at the wrong point fails in one of two directions and both are bad: it does not block something it should, or it blocks output that is fine. Three of these nine are BLOCKING, so getting one wrong stops a company's statements -- and `check_new_ledger_mid_year` as written would fire on every account of a first month, which is what wiring one of them without resolving its threshold looks like in practice. Where a check fires is also a claim about what the check *means*, which is corpus/09's authority, not a caller's.
Downstream blocked: nothing is blocked from running, and that is the point -- every one of these conditions passes silently today. Specifically: `src/quality/checks.py`'s `check_zero_gl_rows`, `check_stream_supplied`, `check_unmapped_value`, `check_opening_balances_present`, `check_duplicate_file_upload`, `check_duplicate_content_different_voucher`, `check_new_ledger_mid_year`, `check_backdated_entries` and `check_margin_sign_flip`; D-053's badge and block bands, enforced nowhere; the exception queue's usefulness as a gate on `src/reports/signoff.py`, `src/api/routes/reports.py` and the period state machine's `blocking_exception_count`; and `GET /data-health`, which names streams as supplied or not without the check corpus/09 section 2.1 assigns to that screen. `check_backdated_entries` additionally waits on OQ-011's scheduler. `cash_flow_ties_to_balance_sheet` waits on OQ-004.

**Resolved 2026-08-31 by Bhavya Singhal, founder.** Option (a): wire each check at the trigger point corpus/09's own Action column names, and resolve the three with no stated trigger individually rather than by a blanket rule. `check_backdated_entries` joins them at load rather than waiting on a scheduler -- see OQ-011's correction, where "every run" in corpus/04 section 5 turns out to mean every load run.
At load, in `src/ingest/load_pipeline.load_gl_file`: `check_duplicate_file_upload`, `check_duplicate_content_different_voucher` and `check_backdated_entries`, joining the trial balance imbalance that is already written there. At statement assembly: `check_zero_gl_rows`. At balance sheet assembly: `check_opening_balances_present`. On `GET /data-health`: `check_stream_supplied`, once the required-stream list has a home -- corpus/09 names the screen and not the list, so the caller supplies it from configuration, per CLAUDE.md section 8's "config, never code."
The three with no stated trigger, decided here and flagged as interpretation rather than corpus: **`check_unmapped_value` fires at the mapping freeze, per period governed by the version being frozen** -- unmapped value changes only when the mapping changes, so evaluating it anywhere else re-computes an unchanged number, and the freeze is where a version's coverage is already assessed. This is a reading of D-053's "period value", not a sentence corpus/09 contains. **`check_margin_sign_flip` fires at pack generation, in the margin analysis section**; note that `src/reports/pack.build_margin_analysis` is a pure function over two P&L results with no connection, and the check queries `fact_channel_order_line` per channel, so it is called from `generate_pack` at the margin-analysis step rather than from inside that function unless its signature widens. **`check_new_ledger_mid_year` is NOT wired**, and that is not deferral: "a new account appears carrying large value" (corpus/09 section 2.7) has no declared threshold, and first-appearance detected as `NOT EXISTS` over prior periods makes every account of a company's earliest period new. Wiring it today emits one WARNING per account on a first month. The threshold is raised as **OQ-019** rather than resolved here.
**Not yet applied.** What this changes: `src/ingest/load_pipeline.py` calls three more checks and passes their candidates to the existing `write_exceptions`; `src/reports/balance_sheet.py` and the statement assembly path call theirs; `src/api/routes/data_health.py` calls `check_stream_supplied` over a configured stream list; `src/mapping/review.freeze_mapping_version` calls `check_unmapped_value`; `src/reports/pack.generate_pack` calls `check_margin_sign_flip`. The consequence recorded in the escalation is what this fixes: the `exception` table gains producers beyond the trial balance imbalance, so the three gates that read it -- the period state machine's `blocking_exception_count`, `src/api/routes/reports.py`'s endpoint and `src/reports/signoff.py`'s corpus/08 section 10 sign-off gate -- start seeing conditions other than an imbalance. D-053's badge band fires for the first time. Two things noted in the escalation are also fixed as part of this: `check_backdated_entries` gains corpus/09 section 2.6's "touching a locked period" qualifier, and `src/quality/checks.py`'s scope-boundary docstring drops the claim that corpus/11 defect #9 is structurally undetectable, which `src/ingest/canonical.py:582` made stale. `cash_flow_ties_to_balance_sheet` stays unwired, downstream of OQ-004.

## OQ-019  "Large value" for a new ledger appearing mid-year is undeclared
Raised: 2026-08-31   Task: wiring corpus/09 section 2.7's new-account check, under OQ-018's resolution
Blocked by: corpus gap
What I need: what rupee value makes a newly-appearing ledger "large" for corpus/09 section 2.7's purposes, and does "mid-year" exclude a company's earliest ingested period?
corpus/09 section 2.7's row is "A new account appears carrying large value", WARNING, "Queued by rupee value". No number is given, and this is not one of the open DECISION-REQUIRED items in corpus/00 -- it is the same shape as OQ-001's auto-accept ceiling and OQ-003's admission caps: a threshold the corpus plainly intends to exist and never states. corpus/06 section 4.4 handles a new ledger appearing mid-year as a mapping event and likewise names no value.
`src/quality/checks.check_new_ledger_mid_year` has no threshold at all, so it flags every account whose first GL activity falls in the period. That is not a defect in the function so much as the honest consequence of having nothing to compare against -- but it makes the check unwireable, and for a second reason beyond the missing number. First appearance is detected as `NOT EXISTS` over prior periods, so in a company's earliest ingested period every account is new. On the synthetic manufacturer, wiring this at load would raise one WARNING per account on 2023-04 and nothing thereafter that a threshold would have filtered. The corpus's own word is "mid-year", which reads as excluding the first period, but nothing states that as a rule and the check has no notion of where a company's history starts.
Options I can see: (a) a per-company rupee floor declared at onboarding, the shape D-065 took for OQ-001's auto-accept ceiling and the one that fits CLAUDE.md section 8's "config, never code"; (b) a floor relative to period value rather than absolute, which self-scales across companies but is circular in the same way OQ-001's resolution rejected -- period value is known at load, so this one is at least computable, unlike the revenue-relative version; (c) no value threshold at all, with "mid-year" alone doing the filtering -- suppress the earliest ingested period and queue every new ledger after it by rupee value, which is what "Queued by rupee value" arguably already describes; (d) a decision id was meant to cover this and is missing from corpus/00.
What I will NOT do: pick a rupee figure, or decide on my own that the earliest period is exempt. Both are thresholds under CLAUDE.md section 3.2, and the second is a threshold disguised as a scoping rule -- suppressing a period is still deciding when a check does not fire.
Downstream blocked: `src/quality/checks.check_new_ledger_mid_year` stays unwired under OQ-018's resolution, which wires the other eight. It is the one catalogue row that produces no exception even after that work, so a genuinely new material ledger arriving mid-year is invisible in the queue. corpus/11 section 2.2's seeded defect #5 (a ledger appearing mid-year with large value) is therefore still undetected by the running system, and `src/mapping/rules.py`'s handling of it is unaffected -- this is about surfacing it as an exception, not about mapping it.
