# Generated from corpus/05 and corpus/05a for metric 'capex'. Do not hand-edit.
source: corpus/05_SPEQULA_METRIC_REGISTRY.csv (registry row only; no worked contract exists in corpus/05a
  for this metric)
registry:
  metric_id: capex
  label: Capital expenditure
  category: CONVENTION
  profile: both
  p0_ask: true
  p0_statement: true
  definition: Investment in fixed assets.
  formula: delta gl_class(asset.fixed_asset_gross) + delta gl_class(asset.capital_wip)
  unit: INR
  source_facts:
  - derived
  dimensions:
  - entity
  - date
  time_grain: month
  time_logic: period_sum
  aggregation: sum
  comparisons:
  - mom
  - yoy
  - ytd
  allowed_filters:
  - entity
  - period
  - customer
  - product
  - channel
  - location
  - cost_centre
  dependencies: []
  alternative_definitions: Cash basis vs additions to gross block
  override_allowed: true
  governed_by:
  - D-037
  unresolved_decisions: []
  compiles: true
  version: 1
  owner: SPEQULA global default, resolved 2026-08-20
